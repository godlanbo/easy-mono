import { expect, test, describe } from 'vitest'
import { FileOperator } from '../src/index'

describe('FileOperator', () => {
  test('happy path', () => {
    const fileOperator = new FileOperator()

    // fileOperator.cd().cd()
  })
})
